const SectionBreaker = () => {
  return (
    <div className="w-full h-px bg-gradient-to-r from-transparent via-border/80 to-transparent my-12 relative">
      <div className="absolute inset-0 bg-gradient-to-r from-transparent via-nst-blue/20 to-transparent h-px"></div>
    </div>
  );
};

export default SectionBreaker;
